[package]
name = "core"
version = "0.1.0"
edition = "2024"

[dependencies]
csv = "1.3.1"
uuid = {version = "1.18.1", features = ["v4"]}
sqlx = {version = "0.8.6", features = ["runtime-tokio", "sqlite"]}
rand = "0.8"
itertools = "0.12"

[dev-dependencies]
paste = "1.0"

